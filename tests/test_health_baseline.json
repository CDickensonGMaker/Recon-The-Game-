{
	"_comment": "Test scripts that NOTHING invokes. run_all_tests.ps1 globs tests/test_*.tscn only, so a probe_* scene is dead weight until it is wired or renamed. THIS LIST ONLY SHRINKS. 'count' and 'ceiling' are the ratchet's witnesses: the probe FAILS if either disagrees with 'uninvoked', so a hand-edit cannot pass quietly. --write-baseline can only ever remove entries. New entries enter ONLY via --grandfather --reason=<text>.",
	"uninvoked": [
		"probe_perf_decay"
	],
	"count": 1,
	"ceiling": 1,
	"grandfather_log": [
		{
			"date": "2026-07-19",
			"reason": "2026-07-19 initial census: 16 probe_*.tscn in tests/ that run_all_tests.ps1 never globs, plus test_world_minimap with no scene. Each needs wiring or deletion, one judgement per probe.",
			"added": [
				"probe_ambush_sites",
				"probe_autoload_reach",
				"probe_concealment",
				"probe_daynight",
				"probe_perf_decay",
				"probe_settlement_spacing",
				"probe_smoke_all",
				"probe_spawn_zoning",
				"probe_tree_cover_wired",
				"probe_veg_density",
				"probe_veg_material",
				"probe_water_channels",
				"probe_water_once",
				"probe_world_rosters",
				"probe_worldbuild_phase1",
				"probe_zoning_histogram",
				"test_world_minimap"
			]
		}
	]
}
