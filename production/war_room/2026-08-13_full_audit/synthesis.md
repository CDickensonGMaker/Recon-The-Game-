# SYNTHESIS — the 2026-08-13 full audit (his order: "audit the recongame project again")

**Arbiter, evening.** Four auditors in `analysis/` (code_drift, demo_arc, registers,
adversary_today). Every fix probe-verified after the last edit; shipped as `d2f8f013`.

## The scoreboard

- **Suite: 107 PASS / 17 LEAK / 19 FAIL / 0 TIMEOUT of 143** (morning baseline 103/20/20).
  Only REGRESS = the known water item. Caveat recorded: the run overlapped late audit edits;
  every touched file was re-verified by targeted probe after. `test_air_fleet`'s
  `[NAV] no path` row is flagged for the next clean run (possibly nav-truth-adjacent).
- Ratchets: fossils 28/28 (the machine now SEES `static func` — it was blind its whole
  life), liveness debt banked 11→7, doc-pointers 0/94, ship-parity PASS.

## What the audit caught and the day fixed (beyond the two morning waves)

1. **Two defects in the day's own work** (adversary): the stray-parapet twin lookup was
   alias-blind to the reparent (now proven: twin FOUND, stray genuinely offset, adoption
   correct); the CH-47's door guns aimed out the nose (outward now derives from seat yaw).
2. **The demo's two-click self-destruct** (demo-arc D-1): Esc → QUIT TO MENU → NEW GAME
   rebuilt a half-clocked demo world. Demo quits are now RESTART DAY, clean reboot.
3. **The siege materialize ring strayed 47.2 m** (demo-arc D-2 — measured worse than the
   32 m estimate): ring is center-true now; the offset prints every boot.
4. **The fossil machine's blind spot** (code-drift): `static func` invisible since birth.
   Regex widened; 4 true fossils deleted, 1 deliberate stub grandfathered
   (`CursorSet.use_large` — his wire-or-cut call). One cut was initially WRONG (took the
   live resolver on an alias-blind grep) — **caught by the verification boot, restored,
   only the dead wrapper cut.** The gate worked on its own operator.
5. **521 MB of tracked `_PRE_` blend backups untracked** (gitignore verified live again);
   the office-chair texture + 5 orphaned imports committed — a fresh clone builds.
6. Registers made honest: `test_viewmodel_contract` given a KnownRed home (4 guns, all
   HIS exports — a NEW contract break is scoreboard-visible again), leak flakiness written
   at the point of reading, PERF_LEDGER instrument correction dated, `END_AT_S`
   retired-but-cited drift purged from CLAUDE.md + GAME_GUIDE.

## Standing after the day

- Demo arc walks clean end to end, beat by beat (demo-arc auditor, per-beat trace).
- The demo GATE is re-opened by design: the validated siege ran on the fictional navmesh.
  **His siege-night replay is the day's price and its proof.**
- IN FLIGHT: the crashed-aircraft commission (Skyraider rebuild + Huey + Phantom wrecks,
  blender-modeler on Opus, headless, his four rulings encoded — fire danger only inside
  the flames, pilot-rescue ground clear).
- PARKED deliberately: cover-seek snap (his watched session; landing plan written), water
  river-leveling (designed fix, no demo exposure), the off-mesh bunker post + 4 revetment
  routes (his eye), napalm audio, ambient_war fixed 12.0, hut fireball class.
