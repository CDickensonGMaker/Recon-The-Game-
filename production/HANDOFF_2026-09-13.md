# Handoff — Conquest of Worms cast wave, 2026-09-11 → 09-13

No code was written in this wave. Everything here is art, tooling, rulings, and what is owed.
Decree + every verbatim ruling: `production/war_room/talking_heads_2026-09-11.md`.
Review page (his artifact, 14 versions): https://claude.ai/code/artifact/4b826dc0-018a-4601-9ff3-288d6bbe3ee3

## Pushed (RECONgame master)
- `2a8cde37` — talking heads + Rhubarb lip sync, full-cell face projection, ears back, ear
  necklace (15 slots, 5 decay stages), six gore piles, WW1 finish pass.
- `daf621cd` — Michael's own face, Gus freckles, sniper's real head talking, skull teeth.

## Uncommitted in the tree right now
- `assets/us/characters/conquest_of_worms_us_cast.blend` — **Caleb's own save, 09-12 23:15**:
  his paint/UV edits + a `REVIEW_APPENDED` collection (sniper + seven WW1 appended for review;
  strip it before any export, never export from it). 26/30 head polys on all ten
  `us_grunt_joined_*` bodies carry the game's canonical `grunt_head` wrap (his ruling "fix all
  the uv wraps to match the grunts"). **Not finished:** 4 front-face polys + the neck ring have
  no canonical partner — match them to `us_grunt_joined` in `us_base_v3.blend` by centre.
- Corpse v2 (`assets/world/props/gore_corpse/`, `tools/gore_corpse/`), renders, notes.
- Whatever the two agents below land.

## Running when this was written (check `production/blender_notes.md` for their entries)

**Outcome (same day, evening — the machine froze and killed both agents):** both landed their
files first. The WW1 pass is logged (`ART_Track_Log.md` 2026-09-13). McCleary + Champs are on disk
(`cow_mccleary.glb`, `cow_mccleary_helmet.glb`, `cow_champs.glb`, `cow_insignia_64.png`, face cells,
`production/hash_gates/`, `tools/add_cow_cast_member.py` / `build_cow_face_cell.py` /
`build_cow_insignia.py`, nine renders) but **UNLOGGED** — no `blender_notes` or ART-log entry, so
their gates and measurements are unknown. Committed with the NPC batch so nothing is lost. The
review page (v15, same URL) now carries both men marked "landed, unreviewed" plus the seven WW1
faces; their `blender_notes` / ART-log entries are still owed.
1. **WW1 quality pass** — `conquest_of_worms_ww1.blend`: rifles ("totally dismantled" — 25-part
   kit, diagnose + assemble), seven DIFFERENT faces (all were one template head), kit/cloth vs
   period photos, grunt wrap on the heads, re-export + re-render + `ww1_FACES_all_seven.png`.
2. **Sgt. McCleary + Champs** — into the cast file, hash-gated against his edits. McCleary:
   bulky, big forearms, wide jaw, cigar, bandana default / helmet alt. Champs: young Black Lt.,
   composed, rank insignia a separate swappable object (Cpl./Lt. drift is HIS call). Exports
   `cow_mccleary.glb`, `cow_champs.glb`, lineup of six.
   Both go on the review page with the corpse v2 when they land.

## The cast on disk
Michael Crawford (new face 9/12), Gus arrival / ears (his face, freckles redone), the sniper
(hand-painted mummified sheet — the grey bearded one is OBSOLETE), seven WW1 figures,
McCleary + Champs (in flight). Cutscene talking heads for Michael, Gus, sniper (his own head,
jaw split at the painted mouth), zombie skull in the ace M1 — `production/cinematics/talking_heads/`,
Rhubarb 1.14 in `tools/rhubarb/` (gitignored), `th_lipsync.py` keys any head from any WAV.

## Rulings that bind the next session
- Cutscene heads must be THE character's head (the generic skull for the sniper was wrong in kind).
- CoW cast uses full-cell head projection + the grunt wrap on matched polys; stock roster untouched.
- Necklace = visible trophy ledger, 15 slots, fifteen real ears at the top bucket.
- Ear stages fresh → days → rotten → dried → old.
- **Six-man piles are dead.** Direction = corpse MATRIX: factions (US/NVA/VC/civilian) × poses
  (supine, prone, propped on a tree, slumped), one command, sprinkled through the AO stamp.
  Gate: his look verdict on the US supine v2 (legs flat, no spun elbows, continuous gut in 5
  hairpins, 150 geometry maggots). Known soft spots: boxy cavity, loops too regular, wound
  hand on the hip not the belly (60° elbow cap — raise to ~90° to put it back).
- **Corpses are interactable:** SEARCH is prompted (hold, weapon lowered ~3 s) → dog tags from
  US dead/pilots, intel from NVA/VC, chance of a necklace charm. **Taking an ear is never
  prompted** — knife, crouch at the head, hold; witnessed per `player.gd:243`. Taught ONCE by
  Gus, diegetically, on the first enemy body searched with him in the squad; never if he's dead.

## Owed — the engine wave (one decree, none started)
- Necklace hang off a Neck socket, slots filled from `CampaignState.ears_taken` buckets.
- Corpse props: 3 Godot rows (`MATERIALS` thatch, a `STRUCTURES` row, `FSB_STRUCTURE_KINDS` +
  `HP_FOR`) or they ship BULLETPROOF; placement in the AO stamp (propped variants need a tree
  anchor); maggot/fly particles on `fx_maggots_01..03` / `fx_flies_01`; the search verb + loot
  table by faction + Gus's one-time line; `loot_socket` on each corpse variant.

## Open calls for Caleb
- Necklace over Gus's rifle sling (it hides 2 of his 5 ears) — asked three times.
- Champs' rank: Cpl. or Lt.
- Michael's new face: keep, repaint yourself, or revert to the old cell (one commit back).
- Corpse v2 look verdict → then "go" on the matrix.

## Traps found this wave
- Agents drop scripts in the shared scratchpad; an `inspect.py` shadowed stdlib and killed PIL.
- Rig `cap_*` gore caps are unusable on static props (2-poly panes; NVA ones have no UVs).
- Every light-skin roster face cell is ONE template head with flecks — "different" faces need
  a different source head (`face_source/newfaceatlas.png`), warped onto the landmark pixels.
- The skull's zig-zag teeth were the mesh (ray-hit sawtooth row), not the paint.
- Rhubarb misreads "aw" on two-word barks; sentence-length lines sync better.
- MGS1 had no mouths; the PSX talking reference is FF8 / Silent Hill shape keys.
