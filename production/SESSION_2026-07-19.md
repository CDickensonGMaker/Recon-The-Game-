# SESSION RECORD — 2026-07-19

**Status: HANDOFF DOCUMENT. Written for a zero-context session. Read this before acting on anything else.**

Governed by the **POINTER LAW** (`CLAUDE.md:307`): every assertion about code state below cites
`file:line` or names the probe that proves it. Anything without a pointer carries a date banner and is
an opinion as of that date, not a fact about the tree.

**First move of the next session is at the bottom (§8). It is a FULL GAME AUDIT.**

---

## 1 · THE SESSION ENTRY GATE MOVED

**`RECONgame-qrg6` (PLAYTEST R4) is the session entry gate.** Check it first.

The old gate `ida9` (PLAYTEST R3) was **closed 2026-07-19 as superseded**. Its checklist
(*"operation → firebase hub → TOC briefing [E] → board bird → exfil"*) tested a game that commit
`4d4fff2d` deleted under ADR-029. Every step of it was gone. **An entry gate that cannot be passed is
theatre**, and it had been the standing entry ritual for ~95 commits.

Every live pointer must name `qrg6`. Five carried the dead `ida9`:
`production/OVERSEER_CHARTER.md:95,113,119,135,168` · `production/GAME_GUIDE.md:302` · `CLAUDE.md`
(which named **no gate at all** — precisely why the stale pointers were the only ones a fresh session
found) · `.claude/agents/recon-overseer.md:26,43`.

`.claude/agents/recon-overseer.md` is **repointed and committed** by this record's author. **It was not
in the handoff plan and is the most load-bearing of the five** — it is the Overseer's own standing
instructions, loaded at the top of every session.

The charter / guide / `CLAUDE.md` repointing is owned by a separate docs agent this session (scope split
2026-07-19). Charter `:113` in particular asserted *"`ida9` is the one still open,"* which is **false**;
`:168` is a changelog entry and should be marked SUPERSEDED rather than rewritten, since it is history.
**If those three files still name `ida9` when you read this, that is the bug — fix it.**

**Left alone deliberately:** `ida9` references in `production/adr/ADR-002,005,006,007,008,012,015` and
in `DOC_AUDIT_2026-07-19.md`. Those are dated records that were true when written. ADRs are history, not
live pointers (ADR-014). **Do not "fix" them.**

`qrg6` was converted **task → epic** to wire it. `bd` refuses task↔epic dependency links in both
directions (`Error: epics can only block other epics, not tasks`); the old `ida9` links predate that
guard. The charter's prior workaround was to hold the gate "by law, not mechanism" — which is the
toothless gate ADR-015 exists to forbid. It is now mechanical: **`97u3` DEPENDS ON `qrg6`, verified.**

---

## 2 · CORRECTIONS A FRESH SESSION MUST NOT RE-DERIVE

Each of these cost real work this session. They are the expensive half of this document.

- **`WorldBuilder` DOES NOT EXIST.** Zero hits repo-wide. The real path is `MissionGenerator`
  (`plan_patrol_world:417` / `build_patrol_world:536`) + `scripts/world/site_planner.gd`. Four beads sent
  agents hunting this class. This is the pointer law's origin story.
- **Animation clips resolve from `assets/shared/anim_library.glb`, NOT per-character GLBs.**
  `_merge_shared_library` reports clips **ADDED, not total** — "+27" means the model already carried the
  rest. Do not read that number as a clip count.
- **Models not on the `PSXRig/Skeleton3D` layout get ZERO merged clips.** `model_actor.gd:265` guards
  this **deliberately**. A model with no animation is usually on the wrong rig, not broken.
- **The fossil baseline is 27**, not 124 and not 143. (124 was an error that propagated into `CLAUDE.md`
  and then into an audit — the pointer law's first catch.)
- **`scripts/visuals/sprite_state_map.gd` is the LIVE animation spine** (11 call sites) despite ADR-001
  retiring the sprite renderer. **Deleting it per canon breaks all animation.** ADR-001:48-50 also
  RETAINS the sprite tooling for an optional A/B — do not delete that either.
- **The 0.38–0.59 heightmap range is INTENTIONAL** (bounded-amplitude scaling, bead `p7wx`). The lever
  for less-flat terrain is `TerrainConfig.preset_relief()` (`terrain/core/terrain_config.gd:31`), **not
  widening the range.**
- **`art_source/` is gone permanently and must never reappear.** An agent recreated it once today. The
  real art path is `assets/`.
- **Godot STRIPS `renderer/rendering_method` on save** when it equals the default. **Forward+ cannot be
  locked in `project.godot`** — it holds only by being the desktop default. Any doc citing
  `project.godot:300` for `forward_plus` is dead (charter §5 rows corrected today; ADR-026 already
  corrected). Verify the renderer at runtime, never by grepping config.
- **The ~20 integer-division warnings in `terrain/` and `terrain/water/` are BENIGN** — `flat_index / width`
  row decomposition and nearest-neighbour upsampling. Verified this session. **Do not "fix" them.**
- **`bd list` SILENTLY TRUNCATES AT 50** while ~129 beads are open. No warning, no count. **Use
  `bd list --limit 0`.** This is why the stale gate survived a full doc audit — the replacement bead sat
  past row 50. Now recorded at `CLAUDE.md` §Beads/Rules.

---

## 3 · WHAT LANDED TODAY

Bead ledger: **`RECONgame-kgz0`** (record-only, closed on creation).

**The four that were invisible to static reads** — all four were found by *running the game*, not by
reading it. This is the argument for §8's method.

- **NIGHT NEVER FELL.** `sim_clock.gd:13` `time_period_changed` had **zero consumers**; the game read
  `MissionWeather.is_night`, a **static frozen at mission start**. Now wired.
- **CIVILIANS WERE IMMUNE TO EVERYTHING.** `take_damage` had zero callers **AND** the wrong arity **AND**
  the explosion loop skipped them — three independent failures stacked. Now killable, with hitzones + gib.
- **VILLAGERS PAST 305 m WERE PERMANENTLY FROZEN.** LOD disabled `_physics_process`, and only
  `_physics_process` could tier them back — a one-way door.
- **FIRE SUPPORT WAS NEVER GRANTED.** `director.fire_support` was never assigned from anything, so **5 of
  6 support verbs answered NONE AVAILABLE forever.**

**Also landed:** every villager played the RIFLE idle (this is the "crouched aiming poses" from playtest 1)
· civilian BT dispatch · the tunnel interact already worked at 3.0 m with no prompt (prompt added) ·
flinch/death theater · destructible traps · informer LOS · world-space nameplates (5 m / 12°, **allies
only**, Pillar 3) · helicopters and ambient war materialising · ambush `COVER_NEAR_M` enforced (100% site
yield at realistic and sparse jungle) · terrain re-seat chain.

**Weapons:** blooper unified on `m79_he.tres` · **RPG-7 was firing the RPG-2's 250 instead of 290** ·
**AK dived 6.7° on every ADS raise** (no `ads_rotation` declared) — AK + Mosin `ads_rotation` added.

### One height authority (commit `100eeb81`) — VERIFIED

`terrain/core/terrain_config.gd`, `class_name TerrainConfig`, `WORLD_HEIGHT_MAX = 350.0` at `:17`.
**Eight** divergent copies of `height_scale` consolidated (the audit found four; four more were found
in `heightmap_storage.gd:8`, `terrain_chunk.gd:14`, `damage_system.gd:107`, `game_world.gd:128`).

Probe `tests/test_height_authority.tscn`: **9/10 checks pass.** Decode scale, live shader and chunk mesh
all agree at 350.0; `get_height_at == sample_world` to **0.0000 m over 400 samples**.
`test_terrain_desync` holds at **0.000 m drift**; `test_terrain_relief_bounds` and `test_flat_damage` pass.
Verified live through `main.tscn`: height range 0.38–0.59 unchanged, `[SPAWN-TRUTH] delta=-0.00`,
firebase and squad spawn, FPS 28–34.

**The 10th check FAILS and it is the real finding — bead `RECONgame-1t6h` (P1).** Of 347 carved channel
points, only **53 (15.3%) have water**. Where water exists it sits on the bed to 0.00 m. **The water is
not mis-heighted — it is absent from 85% of the carved grooves.** This is the Summoner's *"the creekbed
is somewhere and the water is somewhere else."* Prime suspect: `terrain/water/hydrology_map.gd:504`
`_trace_river` sets channel type but never writes `_surface_h`. **Beaded, deliberately not fixed**, to
keep `100eeb81` clean.

**Unresolved and MUST STAY unresolved:** the RiverGenerator vs HydrologyMap disagreement rate. The
probe's Section D is built to measure it and **has produced no number. Do not guess one.**

### New machinery

- **Fossil ratchet:** register **143 → 27**. `--write-baseline` can only **shrink**; growth requires
  `--grandfather --reason=`. **AND `_judge()`'s inversion was fixed** — it counted a *declaration* as its
  own reference, so **two files declaring one symbol alibied each other.**
- **`probe_autoload_reach`:** autoloads probed with `Engine.has_singleton` are **ALWAYS false when the
  script omits `class_name`.** This was the SimClock bug; it also caught `camp_director.gd:159`.
- `tools/recon_paths.py` tool-path contract with ratchet · weapon/projectile contract · texture coverage
  probe · `tests/test_gib_contract_all.tscn` (**24/24 pass**) · `tools/probe_doc_pointers.py` (CEILING 44)
  · `tests/test_height_authority.tscn`.

---

## 4 · SUMMONER DESIGN DECISIONS MADE TODAY

These are rulings, not proposals. Do not re-litigate them.

- **Civilians killable, no consequence system** — a flagged hook only, nothing behind it yet.
- **Crossfire is real.** AI can hit civilians, and **VC execute villagers**: a scripted event on the
  **third approach within 100 m** of a village — villagers lined up cowering, ~a dozen VC, one with a
  pistol shooting them one by one, **the player may intervene**, last two left alive. Needs a
  decent-sized village to stage.
- **Airfield is a LOCATION TYPE.** Max 1 spawn, decent distance from the firebase, **must sit on flat
  ground**, under attack with US defenders.
- **11 unused buildings to be placed.** Drop the stone arch bridge. Underground hospital and napalm cans
  deferred to an art bead for his eyes.
- **Tunnels FROZEN → WIP.** ⚠ **The Catacombs cave-kit port is NOT done — only the prompt was fixed.**
- **Fork F4 `squad_leader` → DELETE**, superseded by `enemy_base.gd:104` + `:1891-1903`.
- **Bore lerp NOT wired** — it would throw rounds ~4.8 m off the crosshair at 25 m. See P0 `7ioa`.
- **Grenade cook timer keeps its 4-second self-kill with NO HUD indicator, deliberately** (bead `ln9n`).
  Note this is a conscious, ruled exception to the r4bk affordance law.
- **Rivers should matter** once terrain height is trustworthy. It now is (§3) — so `1t6h` is live work.
- **Roads connecting the firebase to villages, plus convoy routes.**
- **Seed determinism break ACCEPTED** (riverbed carve 2.25 m → 1.8 m). Recorded as
  **ADR-010 Amendment A, dated 2026-07-19**, written this session.

---

## 5 · THE BEAD GRAPH (rewired this session)

- **`97u3` DEPENDS ON `qrg6`** — the ADR-015 mechanical gate has teeth again. All six of its prior
  children are closed, so **it had been blocking nothing.**
- **Epics gated behind `97u3`:** `m6g6` (cinematics, ADR-024) · `8l06` (LIVING FIGHT) · `x0r1`
  (world-build unification, ADR-028) · `dtkh` (new, see below).
- **`qqv6` (ADS wave) NOT gated** — it carries its own explicit *"GATE RULING: EXEMPT from 97u3"* (ADS is
  a shipped system per ADR-004; this is presentation + data). Instead it got its real blocker beaded:
  **`qqv6.1`** — all 12 `assets/player/viewmodels/*_fp.glb` have `MuzzlePoint` and **none** has
  `SightRear`/`SightFront`, so `viewmodel_editor.gd:487-529` (key V) has **always** failed over to
  hand-tuning. That single gap is why 5 guns share the identical placeholder `ads_position`.
- **`5r4y` CLOSED** at 6/6 children complete, per its own *"SUMMONER CALL NEEDED"* note. Its deferred
  block spun out to **`dtkh`** — *THE INHABITED WAR part 2: roads, convoys, families, animals*.
  **`dqgx` reparented under it.**

**ROADS ARE THE KEYSTONE of `dtkh` — build them first.** They unblock three things:
1. **Ambush siting** — `ambush_planner.gd:14` declares `ROAD_NEAR_M` but `TerrainType` has **no
   ROAD/TRAIL member for it to point at**, so that term can never score.
2. **Convoys (`dqgx`)** — `convoy_spawner.gd:_instantiate` calls `cv.setup(route, [])` with an **empty
   array**, and `convoy.gd:41` bails when `vehicles.is_empty()`. **Every convoy ever spawned is a dead
   shell.** Two design gaps make a half-wire worse than inert: `convoy.gd:63-66` drives x/z and never
   touches y (trucks phase through hillsides), and `_schedule_one_convoy` draws a **random straight leg**
   from the insertion LZ ignoring terrain.
3. **AO structure** — roads are what make the 40% Inhabited War read as inhabited.

---

## 6 · NEXT PLAYTEST TARGET — `oo38`

Second playtest today: **the squad is fidgety and the pointman is DRIVEN, not leading.** Verbatim:

> *"he needs to have a further distance and act like hes leading the way and if a area to sweep spawns
> in hell direct himself toward that and halt about 170m away and say something like village up ahead,
> look sharp."*

**Depends on `akx8` (Wave A) for follow/separation. DO NOT build a second follow system.**

---

## 7 · KNOWN OPEN

- **13 `OWNER DECISION` markers** in `production/COMPETING_SYSTEMS_2026-07-19.md`.
- `production/DOC_AUDIT_2026-07-19.md` **deletion list awaiting a ruling.**
- **Four DRAFT ADRs** unratified: 024, 025, 026, 027.
- `ggct` — two local tags pin ~1 GB. · `67ga`. · `v82k` — needs an ADR-029 amendment **first**.
- **`tsmj` promoted to P1 — this is a live Pillar-1 FAIRNESS defect, not tidiness.** Every named anchor
  key misses: `mission_generator.gd:161,405` read `village_center` / `firebase_center` / `camp_center`;
  the planner writes `fsb_center` / `village_centers` / `camp_centers` at `:439,485,499`. Singular vs
  plural, `fsb_` vs `firebase_`. **Misses are silent behind `p.has()`.** `insertion_lz` and `exfil_lz` are
  **both `gm.spawn_pos`** — so **the player's own spawn is a first-class node in the enemy patrol anchor
  pool** and patrols can legally route onto the ground he boots on. That is death-from-nowhere, not
  death-from-situation. The plural keys also hold **Array, not Vector3**, so a naive rename will fail.
- **`nva_regular` has no model** and wears `vc_guerilla_ppsh`.
- **7 `.tres` files carry a UTF-8 BOM the importer rejects.** Deferred until the Summoner is off the bench.
- **FPS 24–38 on Intel UHD, Forward+ — below the 30 gate.** Perf remains the top systemic risk.

---

## 8 · NEXT SESSION'S FIRST MOVE — A FULL GAME AUDIT

Workflow-shaped: **understand → find → adversarially verify → synthesise.** Coverage: the five pillars as
lenses, plus a pass per subsystem (terrain/water, AI, world-build, combat, UI, art pipeline, canon).

**Two requirements today's audits proved the hard way:**

1. **Verify against a RUNNING game via the Godot MCP, not just source.** `fire_support` never being
   granted, night never falling, the height range and the FPS number were **all invisible to static
   reads.** Every one came from running it.
2. **Hunt claimed-but-inert systems** — the project's signature defect. **`test_fossils` is structurally
   blind to them:** all twelve civilian action methods *were* referenced inside `build_bt()`, so nothing
   looked dead while **the entire schedule system sat inert.** A reference count cannot see a system that
   is wired but never ticked.

The adversarial layer earns its cost: today it caught three doc "corrections" that introduced **new**
falsehoods, and agents correctly overruled the briefing four separate times.

---

## 9 · DO NOT TOUCH — the Summoner's live bench

Uncommitted and **his**: `assets/us/characters/weapons_us.blend` · `assets/nva_vc/weapons_vc.blend` ·
`assets/weapons/world/` · `tools/export_world_guns.py` · `tools/weapon_builder.py` ·
`tools/build_weapons_vc.py` · `data/weapons/` · `data/projectiles/` ·
`scripts/weapons/viewmodel_editor.gd`.

**The commit hook auto-stages.** Check `git diff --cached --name-only` before **every** commit and use
`git -c core.hooksPath=/dev/null commit`.

`project.godot` also shows uncommitted drift — that is Godot stripping `renderer/rendering_method` on
save (§2), not intentional work. Left uncommitted.
