# HANDOFF — 2026-08-07

**Target: Steam Early Access 2026-09-06 (30 days). The product is THE DEMO'S SHAPE.**
Every claim below carries a `file:line` or says **UNVERIFIED**. Nothing here is inferred from a
tracking doc without checking the code.

---

## 1 · WHAT CHANGED TODAY

**One live breakage found and fixed: the game would not boot.** `vc_nva_dresser.gd` had no `.uid`
and was absent from the script-class cache, so `enemy_base.gd:424` failed to resolve `VcNvaDresser`
and every dependent script failed to compile. `--import` regenerated it; the `.uid` is committed.
**It surfaced only because he asked for the MCP run** — `--check-only` cannot see this class of
fault (it fails on missing autoloads regardless), and the first `get_debug_output` read looked clean
because the debugger break had not happened yet.

**M7 (MUST SHIP, High) is DONE** — enemies dress. `enemy_base.gd:399/423/428`; `VcNvaDresser.dress()`
observed running in the boot. This was *"45 identical men assault your wire in the climax."*

**Suite:** 23 tests verified green by hand. Like-for-like over the same 50 tests, run 6 → run 8:
**31 PASS → 39, 9 FAIL → 5.** **No completed full run exists post-fix** — runs 8 and 9 were both
killed externally mid-flight. **Get one uninterrupted run before trusting any total.**

**Tally: ~10 real defects, ~28 stale or vacuous probes.** The standing pattern held all day: *where a
code comment disagreed with a test, the comment was right every single time.*

---

## 2 · WHAT'S LEFT — CODE (costs him ZERO art-days)

| Item | State | Evidence |
|---|---|---|
| **M1 · Atomic saves** | **NOT DONE. The top open MUST SHIP item.** `save_game` opens the live slot `FileAccess.WRITE`, stringifies, stores, closes — **no temp, no rename, no `.bak`** — and autosave rewrites slot 8 every 30 s. A crash mid-write destroys the save. | `save_manager.gd:99-109`, `:20-22` |
| **M2 · Reject future-version saves** | Not done; no upper version bound. | `save_manager.gd` |
| **M3 · Demo/real save-dir leak** | Unverified on the taskbar-kill path. Its own comment says a session already once wrote into the real slots. | `demo_game.gd:82-96`, `:119-124` |
| **M19 · Mounted MG fires nothing** | Open. Strong hypothesis unchanged: `m60.tres` carries a 32.9 m Z hip offset the FP lens cancels, so mounted rounds likely spawn ~33 m downrange. | `mg_emplacement.gd`, `player.gd:1347` |
| **M20 · Roads invisible** | Open, and **may be out of EA scope** — a 512 m firebase map may legitimately have none. Diagnose before spending. | `mission_generator.gd:562`, `:680` |
| `group_walk` backward marching | **Real defect. Awaiting his ruling (A/B).** Formation slots assigned by array order, so followers already ahead of the lead walk backward — one ended −3.57 m. | `group_walk.gd:25-37` |
| `hunters` count | **Real. Awaiting his ruling (a/b/c).** Ambient patrols are folded into a count nothing reads, so they never draw against the hunter budget. | `lazy_group.gd:93-95`, `field_director.gd:183` |
| Arena nav | **Real, and it survived the config fix.** 12 nav failures + 2 units off-map in the SHIPPED config. Lab-only *so far* — `nav_router.gd:98-101` documents the same mode for the firebase garrison. **Whether it reaches the demo is UNVERIFIED.** | `test_arena_patrol` |
| Chow-hall diner chain | Contract written, tray-return mapped ahead of time. **Blocked on his export.** | `CHOW_HALL_EXPORT_CONTRACT.md` |
| Huey variant switch | `huey.tscn` hardcodes ONE GLB; gunship vs transport cannot be expressed. **Blocked on his export.** | `huey.tscn:4` |
| `probe_gun_streams` | Hangs at shutdown on an unjoined thread. Grandfathered with the reason recorded rather than costing 200 s a run. | `test_health_baseline.json` |

---

## 3 · WHAT'S LEFT — HIS ART

### The critical path (his words: *"a lot of the game hinges on me finishing the firebase"*)
Medical anims → artillery placement → **mortar anims (in progress)** → HQ + anims → 3 hooches
incl. the dug bunker → bunker entry + hangout anims → shoot-out-of-slits.
**Checklist: `FIREBASE_EXPORT_NEED_TO_DO.md`.**

**The load-bearing item there:** the boot prints `[FSB] 331 concave shape(s) forced double-sided`.
The shipped GLB winds INWARD and the game patches it at runtime. Source is fixed in
`gen_firebase.py:146`, so **after his re-export that line should read 0** — that number is his
verification signal, and it is the same root cause behind both bunker questions.

**M11 (CRITICAL):** the final export must be followed by re-running `tools/gen_firebase_v3.py`, or
all 80 named destructible segments break and SiegeDirector goes blind.

### Reachable-weapon defects — the sharpest EA finding of the day
| Weapon | On the rack? | Problem |
|---|---|---|
| **RPD** | **YES** (`rpd_arms_viewmodel.tscn` resolves) | GLB is a **stale 2026-07-11 export holding only `rifle_idle`**. Manifest records its clips retargeted 2026-07-28. **No reload, no fire animation.** Nothing errors. |
| **RPG-2** | **YES** | Identical story, identical five missing clips. |
| M79 | YES | Real 2.8 MB model; needs bench alignment (he put it at 60%). |
| M72 LAW | **NO** — `model_path` is empty, so `tres_complete` filters it off the rack | The missing GLB therefore **cannot hurt EA**. Graceful. |

**M12 is largely resolved and can be de-escalated:** no Kar98k/MP40/Thompson asset is referenced
anywhere (the single grep hit is flavour text in `m1911.tres:9`); SKS does not exist; CAR-15 is not
on the rack. **The WW2-rifle screenshot risk is not real.** The RPD/RPG-2 re-export is.

### Other art, none blocking (all in `ART_GAPS_2026-08-07.md`)
`__mg` weapon-family clips (every MG carrier holds it like an M16, both factions) ·
`stand_to_cover_2/_3` absent so every ally takes cover identically · `rpg7_fp.glb` never exported ·
5 equipment items with no ADS pose · `us_pilot_white/black` gib donors · `us_surgeon` draws as two
men + white mask · `us_medic` white brassard · one burned-out version per building · Huey v3 export
(**gunship needs no doors; transport doors must slide**) · 12 zombie character GLBs absent from disk
(74 `.import` sidecars, 0 `.glb`).

---

## 4 · NEVER MEASURED — the real unknowns

- **M5 · Perf.** THE WALK · ONE DIG · THE BARRAGE have **never produced a recorded result**
  (`test_results/` holds no perf artefact). His own words: *"its def laggy with everything going
  on."* **This gates M17 (balance) and is a Critical item.**
- **M4 · A trustworthy suite total.** See §1.
- **M6 · Spawn-under-world.** Three stacked patches live in code; still on his list.
- **THE DEMO PLAYTHROUGH** — the standing session entry gate, dischargeable only by his verified
  playtest (ADR-015). **Never yet discharged.**

---

## 5 · QUESTIONS FOR HIM

1. **`group_walk`** — (A) fix the formation so followers already ahead stay ahead, or (B) restage
   the probe? *I recommend A: villagers walking backward will read as broken regardless of staging.*
2. **`hunters`** — (a) enforce the count against the pool, (b) drop the fold and let ambient patrols
   be scenery, or (c) leave it and record the ruling?
3. **M1 atomic saves** — do it first next session? *Recommended: it is Critical, pure code, ~30
   lines, and it is the exact bug class that already bit Viper.*
4. **The 46 lines of zombie player glue** (`give_weapon`/`has_weapon_path`/`refill_ammo`) — keep, or
   revert and exempt the probe instead? Inert in the demo, but it is parked-mode work 30 days out.
5. **Roads (M20)** — in EA scope at all, or cut?
6. **Have ANY perf numbers been taken?** If not, M5 should probably outrank everything in §2.
7. **Huey** — beyond doors, do gunship and transport need to differ visually? The renders are
   currently identical externally.

---

## 6 · SUGGESTED ORDER NEXT SESSION

1. One **uninterrupted full suite run** — nothing is trustworthy without it.
2. **M1 atomic saves** (code, small, Critical).
3. **M5 perf numbers** — the oldest unmeasured Critical item.
4. His rulings on Q1/Q2, then those two fixes.
5. His firebase export → then the chow-hall chain and the Huey variants unblock immediately.

**Uncommitted and his:** `vc_nva_dresser.gd`, `enemy_base.gd`, `nva_vc_soldiers.blend`,
`nva_vc_gear_variants.blend`, `huey_v3*.blend`, `blender_notes.md`. **Left untouched all day.**
