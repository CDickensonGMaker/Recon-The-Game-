# DEMO TIGHT 40 — the approved plan (2026-08-14, his "yes" + perf addendum)

Forty steps to a tighter demo, in six blocks. **His approval:** summary approved 2026-08-14
night, with the addendum: "investigate more on how to optimize... now that we have a clean
system would be a good time to check for fps rates... could use the stress test and fill it
with planes and units and explosions and combat" — folded into Block 3, expanded.
Rules that bind every step: probe-before-claim, file:line or it's an opinion, his eye
discharges feel (ADR-015), fossil law on every touched file, suite at wave ends only.

## BLOCK 1 — VERIFY THE FOUNDATION (1–6)

1. **His siege-night replay checklist** — one watched run discharges: honest navmesh
   (berms/bunkers), breach usage (squads through satchel holes), hooch burst-and-burn,
   Chinook ramp egress + world handoff, napalm chain read, center-true spawn ring
   (`[SIEGE] aim offset` prints), probe felt-identity (mortar volley at 1440s). Write the
   one-page checklist he plays against; everything below assumes this run's verdicts.
2. Clean full-suite baseline on a quiet box (no concurrent edits — the 8/13 run overlapped
   the audit's late fixes); record it in the backlog as the standing post-audit baseline.
3. Adjudicate `test_air_fleet`'s `[NAV] enemy on baked region 0 … no path` row: pre-existing
   or nav-truth fallout. Fix or KnownRed with reason.
4. The honest nav losses: the 1 OFF-MESH bunker post and 4 `fb_bunker_revet_*` capsule
   blocks (posts 0/6/14/35) — fix (revet collider shape or doorway) or rule scenery.
5. Leak-column triage note pinned to the suite header (done 8/13) — verify the KnownRed
   `test_viewmodel_contract` XFAILs on the next clean run and the count reads 143 honest.
6. Save-path standing check for the demo shape: RESTART DAY reboots clean (no half-clock
   carryover), `--test-save` sandbox verified, campaign slots untouched by demo runs.

## BLOCK 2 — FEEL, HIS STANDING DIRECTIONS (7–14)

7. **Cover-seek lands** at his next watched session — the one-hour plan in
   `war_room/2026-08-13_nav_truth_wave/analysis/devils_advocate.md` (Y-flattened snap,
   clamps, shared-path ally check, SFR --cover-probe before/after numbers).
8. **AI look-and-move pass I (measure):** film the worst offenders in the arena — turn
   snap, foot-slide, arrival pops, stance blends; write the ranked defect list with clips.
9. AI look-and-move pass II (fix the top five) — turn-rate smoothing, arrival alignment,
   stance-blend windows; derobotise recipe on the worst clips (`fp-arms-derobotise` precedent).
10. **Napalm audio bank** — source/author a whoosh-ignite + roar set; wire via
    `_AUDIO_KIND`; the borrowed heavy-arty boom ×9 is the loudest remaining nuke tell.
11. Heavy (~46m) + mortar (~28m) sizes re-ruled by his eye on the fire range with the
    `[`/`]` knob; bank his numbers into `_KIND_SCALE`.
12. `ambient_war.gd` fixed scale 12.0 horizon events — verify they still read at 200–800m
    against the re-anchored ladder; retune if the horizon war went quiet.
13. Hooch burned-model slot-in when his per-building art lands (`RUIN_FOR` is ready);
    until then verify the generic burned hut reads at the mortar-burst swap.
14. Garrison motion variety — the camp must not idle in unison (stagger schedule ticks,
    vary idle clips per `_idle_seed`); one probe screenshotting five men proves variety.

## BLOCK 3 — THE PERF CAMPAIGN (15–22) [his addendum, expanded]

15. **The three demo poses** with the working PERF MS instrument: THE WALK · ONE DIG ·
    THE BARRAGE — CPU vs GPU split per pose, into `PERF_LEDGER.md` (first rows ever).
16. **The stress crucible:** arena filled per his spec — 18v18 plus air traffic overhead,
    support fires cycling (napalm run + arty barrage + CBU), tree breaks, full VFX — a
    scripted 3-minute worst-case with frame-ms capture (worst frame, 1% lows, not avg).
17. Attribute the crucible's frame: the four `report_cpu_bucket` feeds + render ms; name
    the top three costs with numbers.
18. **Set the gating FPS number** — the charter's #1 named systemic risk, unset since
    July. Propose floor + measure point (crucible worst-frame on this box; Intel-UHD
    derating factor documented); HIS ratification makes it law and the suite gains the gate.
19. Fix cost #1 found by 17 (measured before/after on the same crucible).
20. Fix cost #2 the same way. (Content-first law: only measured, demonstrable wins ship.)
21. Chunk-rebuild coalescing DECISION with real numbers: the reverted batching from 8/13 —
    measure `rebuild_chunk` cost on the crucible frame; if real, batch on the
    VegetationManager side per the standing note; if not, close the item.
22. Re-run the crucible + the three poses after 19–21; ledger the delta; regression gate
    armed (the suite's perf canary reads the gating number from 18).

## BLOCK 4 — PRESENTATION (23–30)

23. r4bk sweep of every demo-live system: list each simulated system against its HUD
    affordance; anything invisible gets a row (the law: no affordance = doesn't exist).
24. Detection pip decision — the one unshipped stealth affordance; build it or he rules it
    post-EA (it predates the demo scope).
25. End card pass: timing off `siege_ended`, the gunship flight as the last image
    (verified protected), music/sting, restart affordance.
26. Day-night seam by eye: the DAY snap at T+47s (his "leave it and judge" stands — this
    step is the judging), the night ratio drop, illum reads in true dark.
27. Toast/radio voice consistency pass — one narrator register across FieldDirector,
    CampMortar, PilotRecovery, the siege (they accreted separately).
28. Ammo/health/stance readability at 1280×720 (his period-HUD decree conformance).
29. START_HOUR beat: watch the gate-clear-at-day-snap read; move only if his eye says.
30. Wreck/aircraft/vehicle v2 fleet integration: demo sky flies the v2 airframes, motor
    pool parks the v2 vehicles, wreck sites use the fixed wrecks; flip band-aids removed
    for v2 scenes only; collision_table rows measured.

## BLOCK 5 — CONTENT COMPLETENESS, HIS BENCH LIST SEQUENCED (31–36)

31. M72 LAW viewmodel export (`model_path` empty; manifest declares RIG_M72_LAW) — his
    Blender + `python tools/export_all_viewmodels.py m72_law`.
32. RPD + RPG-2 re-export (`python tools/export_all_viewmodels.py rpd rpg2` — the manifest
    gate passes now); RPG-7 viewmodel decision (also empty, unnamed in the reports).
33. The three re-aims on the bench: m60 / m79 / shotgun (spans 35.7 / 11.4 / 59.0 print
    every run); ak47/mosin ADS done 8/13 — the register only shrinks.
34. Blender renames: med/chow soft+destructible ruling; the 242 hard `fb_hwall_*` walls;
    medic brassard red cross + the threshold nudge.
35. Ladder MESH for the four towers (7.4m pairs, climb works, no mesh exists) + HQ +
    village interiors/cabinets per his queue.
36. Non-gun handheld exports with hit-placement markers (his side, from the morning list).

## BLOCK 6 — SHIP MECHANICS (37–40)

37. **Fresh-player protocol** (his law): a written 20-minute script for someone who knows
    nothing — what they must discover unaided (move out, night, the attack, survive);
    run it with a real fresh player; their confusion list becomes the final polish queue.
38. Exported build: `export_presets.cfg` → `build/RECONgame.exe`, smoke-tested from the
    exe on this box (boot → bunk → T+120s clean), size + first-launch time recorded.
39. Full-run error sweep: one complete demo run's log, zero unexplained ERROR/SCRIPT
    ERROR lines (explained ones documented); the AUDIT-12 leak class excepted as known.
40. **The gate itself:** his end-to-end verified playthrough (ADR-015) — boot on the bunk
    to end card, player lives, and his sign-off closes the demo scope. Nothing above
    ships "done" until this run says so.

---
*Ordering note: Blocks run roughly in sequence but 31–36 (his bench) interleave at his
pace, and Block 3 runs on a quiet box between build-chain works. The overnight build
chain (wreck fix → A-1 v2 → F-4 v2 → AC-47 v2 → wreck re-derive → vehicle pass) feeds
step 30 and runs independently of this plan's approval.*
